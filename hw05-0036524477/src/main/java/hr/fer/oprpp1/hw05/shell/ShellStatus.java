package hr.fer.oprpp1.hw05.shell;

/** A enum that contains all the possible shell statuses */
public enum ShellStatus {
    /** Says that the shell should continue reading */
    CONTINUE,

    /** Says that the shell should stop reading */
    TERMINATE
}
